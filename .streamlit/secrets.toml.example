# Beispiel-Datei – NICHT committen!
# Für lokales Testen: Diese Datei kopieren als .streamlit/secrets.toml
# und deinen echten API-Key eintragen.
#
# Für Streamlit Cloud: Den Key direkt im Dashboard unter
# App Settings → Secrets eingeben (kein secrets.toml nötig).

INFOMANIAK_API_KEY = "dein-api-key-hier"
